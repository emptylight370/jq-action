# emptylight370/jq-action Issue Triage Context Card

> Generated by the issue-triage skill on 2026-09-12, reviewed by the user.

## Repository Info

- **owner/repo**: `emptylight370/jq-action`
- **Default branch**: `main`
- **Issue language convention**: 跟随 issue 作者的语言回复（检测不到时默认英文；仓库有双语 README）

## Issue Label List

> Pulled from `gh label list` on 2026-09-12. Labeling may only use labels from this list; creating a new label requires user consent first, followed by updating this list.

| Label | Semantic description |
|-------|----------------------|
| bug | Something isn't working |
| documentation | Improvements or additions to documentation |
| duplicate | This issue or pull request already exists |
| enhancement | New feature or request |
| good first issue | Good for newcomers |
| help wanted | Extra attention is needed |
| invalid | This doesn't seem right |
| question | Further information is requested |
| wontfix | This will not be worked on |
| dependencies | Pull requests that update a dependency file |
| github_actions | Pull requests that update GitHub Actions code |

## Project Conventions

- **Code structure / key directories**: GitHub 复合 Action，全部逻辑集中在 `action.yml`（inputs: `data`/`filter`/`raw`/`options`，outputs: `result`/`multiline`，bash 内联脚本，输入经 env 传入避免注入）；`test/theme.json` 为测试数据；`.github/workflows/`（test.yml 三平台矩阵、release.yml 标签触发）；`mise.toml` + `.mise/tasks/*.ps1`（PowerShell 任务）；`lefthook.yml`、`cliff.toml`、`metadata.json`
- **Mandatory checklists for changes**: 提交信息遵循 Conventional Commits 并引用 issue 号；修改 `action.yml` 或测试工作流后需在 GitHub Actions 上跑通 test.yml 矩阵（本地无测试运行器）；YAML/Markdown 变更需通过 prettier 格式化（`mise` 管理）；多行输出须使用 `result<<DELIMITER` heredoc 策略写入 `$GITHUB_OUTPUT`
- **Lint command**: `prettier --check`（工具由 `mise.toml` 管理，需 `mise install`）
- **Test command**: 无本地测试；`.github/workflows/test.yml`（push 到 main 时若 action.yml/测试工作流变更自动触发，支持 workflow_dispatch 手动触发）

## Change Red Lines

<!-- Files/behaviors that must not be touched -->

- `metadata.json` 的 `version` 字段及发布流程（`mise run tag`、release.yml）：版本变更只由用户手动进行，agent 不得改版本号或代打标签
- `changelog.md`：由 git-cliff 经 pre-commit 钩子自动生成，agent 不得手改
- `lefthook.yml`、`cliff.toml`、`mise.lock`：changelog/钩子配置，未经用户同意不得改动
- `action.yml` 的 shell 注入防护模式（输入经 env 传递）：任何修改不得退化回直接字符串插值

## Closure Strategy

- **When an issue may be closed**: 永不自动关闭 —— 任何情况都不自动关闭 issue，一律由用户手动关闭；agent 可建议关闭但须留待用户执行
- **Intermediate-state label**: 不适用（因永不自动关闭，不使用中间态标签推进关闭；如需标记待验证可经用户同意后手动打标）

## Commit Strategy

<!-- Delivery form for code changes: direct commit, or new branch with a PR; default to PR when undeclared -->

- 直接提交：代码修改经用户确认 diff 后，在当前分支直接 commit；提交信息遵循 Conventional Commits 并引用 issue 号（如 `fix: xxx (#123)`）；不代推送远程之外的写操作（push 前须经用户确认）

## User Preferences

<!-- Obtained by asking the user, not from repository inspection; applied directly on subsequent runs without re-asking -->

- **Progress tracking**: enabled
- **Progress directory**: `.codebuddy/issue-triage/progress/`（每个 issue 一个文件，如 `<issue-number>.md`，记录阶段：digested / awaiting decision / executed / closed out 及关键决策备注）
- **Other preferences**: 无
